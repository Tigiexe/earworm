# Caddy gets and renews the HTTPS certificate by itself.
# Put this in /etc/caddy/Caddyfile, replace the domain, then: sudo systemctl reload caddy
earworm.example.com {
	reverse_proxy 127.0.0.1:8000
}
